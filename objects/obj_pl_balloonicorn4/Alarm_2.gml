if (saved == 0)
{
    alarm[3] = 300
    with (instance_create(120, -120, obj_pl_dynamite1))
        saved = other.saved
    with (instance_create(520, -120, obj_pl_dynamite1))
        saved = other.saved
}

